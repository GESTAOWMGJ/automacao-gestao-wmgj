rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // O MVP não permite acesso direto a evidências. Upload e download usam
    // endpoints backend auditados e URLs assinadas de curta duração.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
